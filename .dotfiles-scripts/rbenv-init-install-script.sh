# This script assumes that rbenv has already been installed via Homebrew
# If rbenv is not already installed then run 'brew update' and 'brew install rbenv' first.

rbenv init

echo "===== manually add '' to the .bash_profile or .zshrc file ====="
echo ""
echo ""
echo ""
echo "===== Next: Generate new SSH keys and sync with Github ====="
echo "===== 'curl -Lks http://bit.do/cfsanderson-ssh | bash' ====="
